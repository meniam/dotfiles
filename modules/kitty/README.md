# Kitty

[Kitty](https://sw.kovidgoyal.net/kitty/) is a GPU-accelerated terminal emulator with keyboard-driven window and tab management.

The module installs Kitty and links its custom configuration, including the split-toggle script in `config/.config/kitty/scripts/`.
