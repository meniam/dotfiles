description="Cross-platform baseline command-line tools"
platforms="mac linux"
deps=""
default="on"
probe="command -v wget >/dev/null && command -v curl >/dev/null && command -v htop >/dev/null && command -v screen >/dev/null && command -v micro >/dev/null && command -v make >/dev/null && command -v just >/dev/null"
