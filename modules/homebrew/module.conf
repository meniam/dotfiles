description="Homebrew package manager, metadata refresh, and Brewfile snapshots"
platforms="mac"
deps=""
default="on"
# The installer bootstraps Homebrew before the first module, so the probe checks
# both the binary and the snapshot helper this module stows.
probe='command -v brew >/dev/null && test -x "$HOME/.local/bin/brew-snapshot"'
