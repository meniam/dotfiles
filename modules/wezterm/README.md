# WezTerm

[WezTerm](https://wezterm.org/) is a GPU-accelerated terminal emulator and multiplexer configured in Lua.

The module installs WezTerm and links the included Lua configuration; on macOS it uses the Homebrew cask, while Linux uses an already configured APT source.
