description="Git and GitHub command-line tooling, including the Lazygit terminal UI and delta"
platforms="mac linux"
deps=""
default="on"
probe="command -v git >/dev/null && command -v gh >/dev/null && command -v git-lfs >/dev/null && command -v delta >/dev/null && lazygit --version >/dev/null"
