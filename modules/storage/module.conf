description="Interactive disk usage and free space analysis tools"
platforms="mac linux"
deps=""
default="off"
probe='command -v ncdu >/dev/null && command -v dust >/dev/null && command -v duf >/dev/null && { [ "$(uname -s)" = Darwin ] || command -v pydf >/dev/null; }'
